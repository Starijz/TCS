# Папка с зависимостями
/node_modules

# Папка со сборкой проекта
/www

# Скомпилированный файл в корне (если случайно создастся)
/index.js

# Логи и системные файлы
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Настройки редакторов кода (опционально, но полезно)
.vscode
.idea
