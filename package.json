{
  "name": "team-color-sorter",
  "version": "1.0.0",
  "description": "An application to sort a list of names into color groups.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.tsx --bundle --outfile=www/index.js --minify --platform=browser && copyfiles index.html manifest.json service-worker.js www"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "html-to-image": "^1.11.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.21.4",
    "typescript": "^5.5.2"
  }
}